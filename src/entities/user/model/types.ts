// entities/user/model/types.ts
// JSONPlaceholder /users resursi — domain turi.

export type User = {
  id: number;
  name: string;
  username: string;
  email: string;
  phone: string;
  website: string;
  companyName: string;
};
